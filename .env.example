# Supabase (server reads VITE_* for backward compatibility, or use explicit names below)
SUPABASE_URL=
SUPABASE_ANON_KEY=
# Optional public fallbacks:
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Session signing for httpOnly cookie (min 32 characters; use a cryptographically random value)
SESSION_SECRET=
